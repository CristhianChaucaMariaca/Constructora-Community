<!DOCTYPE html>
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        block head
            include ../blocks/head
    body
        block nav
            include ../blocks/nav
        block content
        block footer
            include ../blocks/footer
        